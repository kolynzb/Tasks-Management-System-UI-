import React from 'react'

const x_stack = ({children}) => {
  return (
    <div style={{display: "flex", alignItems: "center", width: "85%", justifyContent: "center"}}>
      {children}
    </div>
  )
}

export default x_stack
